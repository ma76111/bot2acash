# 🚀 دليل البدء السريع - Egypt Easy Cash Bot

## 📋 المتطلبات

- Node.js (الإصدار 14 أو أحدث)
- حساب Telegram Bot (احصل على التوكن من @BotFather)
- معرف الأدمن (Telegram User ID)

---

## ⚙️ الإعداد الأولي

### 1. تثبيت الاعتماديات
```bash
npm install
```

### 2. إعداد ملف البيئة
انسخ `.env.example` إلى `.env` وعدّل القيم:
```bash
cp .env.example .env
```

### 3. تعديل config.js
افتح `config.js` وعدّل:
```javascript
BOT_TOKEN: 'YOUR_BOT_TOKEN_HERE'
ADMIN_ID: 'YOUR_TELEGRAM_ID_HERE'
BOT_USERNAME: 'your_bot_username'
```

---

## 🚀 التشغيل

### Windows:
```bash
start-bot.bat
```

### Linux/Mac:
```bash
chmod +x start.sh
./start.sh
```

### يدوياً:
```bash
node bot.js
```

---

## 👑 أول استخدام (للأدمن)

1. افتح البوت في Telegram
2. اضغط `/start`
3. اختر اللغة المفضلة
4. ستظهر لك لوحة تحكم الأدمن

### إضافة حسابات إيميل:
1. اضغط "➕ إضافة يوزرات"
2. أرسل الحسابات بالصيغة:
   ```
   email:password:firstname:lastname
   ```
3. مثال:
   ```
   john@gmail.com:pass123:John:Smith
   mike@gmail.com:pass456:Mike:Johnson
   ```

---

## 👥 للمستخدمين

### التسجيل:
1. افتح البوت
2. اضغط `/start`
3. اختر اللغة
4. اختر العملة (EGP أو USD)

### بدء المهام:
1. اضغط "📋 المهام"
2. اختر نوع المهمة:
   - 📧 مهمة إنشاء يوزرات (5 ج)
   - 📱 مهمة إنشاء جيميل (10 ج)
3. اتبع التعليمات

### نظام الإحالة:
1. اضغط "🔗 نظام الإحالة"
2. اضغط "🔗 كود الإحالة"
3. شارك الرابط مع أصدقائك
4. احصل على:
   - 4 ج عند إكمال صديقك أول مهمة
   - 1.75 ج على كل إيميل ينشئه

---

## 🎮 لوحة تحكم الأدمن

### الأزرار الرئيسية:

#### 👥 إدارة المستخدمين
- 🔍 البحث عن مستخدم
- 📊 آخر 10 مستخدمين
- 📋 تقرير مستخدم كامل

#### 📊 الإحصائيات
- عدد المستخدمين
- الرصيد الإجمالي
- عدد الحسابات المتاحة

#### 📧 اليوزرات المنشأة
- مراجعة الحسابات المرسلة
- موافقة/رفض

#### 📱 مراجعة الجيميلات
- مراجعة حسابات Gmail
- موافقة/رفض

#### 💳 طلبات السحب
- مراجعة طلبات السحب
- موافقة/رفض

#### ⚙️ إعدادات النظام
- 💰 إعدادات المكافآت
- 💳 الحد الأدنى للسحب
- 💱 سعر الصرف
- 💬 رسالة الدعم
- 📢 إشعارات اليوزرات الجديدة

#### 🎮 التحكم في المهام
- تفعيل/تعطيل مهمة اليوزرات
- تفعيل/تعطيل مهمة الجيميل
- تفعيل/تعطيل التحقق من IP المصري

#### 👑 إدارة الأدمنز (للأدمن الرئيسي فقط)
- ➕ إضافة أدمن
- 📋 قائمة الأدمنز
- ❌ حذف أدمن

---

## 🔧 الإعدادات المهمة

### في config.js:

```javascript
// المكافآت (بالجنيه المصري)
TASK_REWARD: 5,                      // مكافأة مهمة اليوزرات
GMAIL_TASK_REWARD: 10,               // مكافأة مهمة الجيميل
REFERRAL_REWARD_EGP: 4,              // مكافأة الإحالة الأساسية
REFERRAL_PER_EMAIL_REWARD_EGP: 1.75, // مكافأة كل إيميل

// السحب
MIN_WITHDRAWAL: 50,                  // الحد الأدنى للسحب

// سعر الصرف
USD_TO_EGP_RATE: 48,                 // 1$ = 48 ج

// كلمة مرور الجيميل الموحدة
GMAIL_PASSWORD: 'Egypt@2024',
```

---

## 🔄 إعادة تعيين البوت

### مع نسخة احتياطية (موصى به):
```bash
node reset-bot-with-backup.js
```

سيتم:
1. إنشاء نسخة احتياطية تلقائياً
2. حذف جميع البيانات
3. تنظيف قاعدة البيانات

### استعادة النسخة الاحتياطية:
```bash
cp backup_TIMESTAMP_bot_database.db bot_database.db
```

---

## 🛡️ الأمان

### الحمايات المطبقة:
- ✅ حماية من تكرار الحسابات
- ✅ حد أقصى 5 إلغاءات للمهام/ساعة
- ✅ حماية من الضغط المزدوج
- ✅ حماية من السحب المزدوج
- ✅ Rate Limiting (30 طلب/دقيقة)

### درجة الأمان: 10/10 🛡️

---

## 📞 المشاكل الشائعة

### البوت لا يستجيب:
1. تأكد من صحة التوكن في config.js
2. تأكد من اتصال الإنترنت
3. تحقق من logs/errors.log

### خطأ في قاعدة البيانات:
```bash
node reset-bot-with-backup.js
```

### المستخدم لا يستطيع استخدام البوت:
1. تحقق من أنه غير محظور
2. تحقق من أن المهام مفعّلة

---

## 📚 المزيد من المعلومات

- **التوثيق الكامل:** `README.md`
- **تقرير الأمان:** `SECURITY-AUDIT-FINAL.md`
- **سجل التحديثات:** `CHANGELOG.md`

---

## 🎉 جاهز للانطلاق!

البوت الآن جاهز للاستخدام. استمتع! 🚀

**الإصدار:** 3.0  
**آخر تحديث:** 2026-04-26
